module.exports = {
  useTabs: false, // 缩进不使用tab，使用空格
  tabWidth: 2, // 缩进字节数
  printWidth: 100, // 超过最大值换行
  singleQuote: true, // 使用单引号
  trailingComma: 'none', // 在对象或数组最后一个元素后面是否加逗号（这里选择不加）
  bracketSpacing: true, // 是否在对象属性添加空格，这里选择是 { foo: bar }
  semi: false // 格式化不加分号
}
